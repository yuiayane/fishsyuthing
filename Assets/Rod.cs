using UnityEngine;

public class Rod : MonoBehaviour
{


    public void Sloow(Vector3 pos)
    {
        transform.position = pos;

    }
    public void Return(Vector3 pos)
    {
        transform.position = pos;
    }

 
}
